/**
 * @deprecated Use `@shelchin/svelte-theme` instead
 * This re-export is kept for backwards compatibility
 */
export { createThemeStore, useTheme, getThemeFromCookies } from '@shelchin/svelte-theme';

export type {
	Theme,
	ThemeContext,
	ThemeStoreOptions,
	UseThemeOptions
} from '@shelchin/svelte-theme';
