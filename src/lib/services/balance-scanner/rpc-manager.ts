/**
 * @deprecated Use `@shelchin/rpc-manager` instead
 * This re-export is kept for backwards compatibility
 */
export { RPCManager } from '@shelchin/rpc-manager';
export type { RPCEndpoint, RPCStatus, RPCManagerConfig } from '@shelchin/rpc-manager';
